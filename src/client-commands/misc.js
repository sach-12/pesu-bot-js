// commands anyone can use
const config = require('../config.json');
const clientInfo = require("./clientHelper");

class Misc {
    constructor() {
        this.commands = [

        ];
    }

    
}
const misc = new Misc()

module.exports = misc